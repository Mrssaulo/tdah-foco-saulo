# Copie este arquivo para .env.local (para testes) e configure
# o mesmo valor no painel da Vercel (Settings > Environment Variables)
# quando for publicar.

OPENROUTER_API_KEY=sk-or-v1-SUA-CHAVE-AQUI
